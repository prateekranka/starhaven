# Starhaven design system

Source: `css/tokens.css`. Gallery: `?kit=1`.

Use `--color-*`, `--space-*`, `--touch-min` (44px). Components: `.ui-btn-primary`, `.ui-panel`, `.ui-dropdown`, `.ui-slider`, `.ui-toggle` (64x44), `.ui-text-field`, `.ui-icon-btn`, `.ui-modal`.

No raw hex on new screens. Music slider hidden until #32.
